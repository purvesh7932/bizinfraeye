<?php
function ResetSalesOrderCreatedQty($entityData) {
    // global $adb;
    // $data = $entityData->{'data'};
    // include_once('include/utils/GeneralConfigUtils.php');
    // foreach ($data['LineItems'] as $key1 => $lineItem) {
    //     $parentLineId = $lineItem['failedpart_lineid'];
    //     $query = "UPDATE vtiger_inventoryproductrel SET so_creatable_qty=? WHERE lineitem_id=?";
    //     $adb->pquery($query, array(0, $parentLineId));
    // }
}
